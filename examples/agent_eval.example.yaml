adapter: __CURRENT_AGENT_ADAPTER__
model: __CURRENT_AGENT_MODEL__
mode: agent
scenarios: scenarios/mvp
out: runs/self_eval_current_agent
temperature: 0.0
max_output_tokens: 1200
timeout_seconds: 60
max_retries: 1
notes: Replace placeholders automatically if your coding agent can infer its current runtime, or use `python -m lexcapital self-eval` / the render-next loop.
